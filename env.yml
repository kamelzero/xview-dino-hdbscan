name: dino-pol
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - pillow
      - opencv-python
      - pyarrow
      - pandas
      - tqdm
      - hdbscan
      - umap-learn
      - matplotlib
      - seaborn
      - scikit-learn
      - timm

